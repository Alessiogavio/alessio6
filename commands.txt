COMANDI GIT: 
t init -> (inizializzazione di progetto)
git video -> (cartella vuota)
ls-> (visualizza dati)
ls-a -> (visualizzare file nascosti)
git status -> (controllo stato)
git add .-> (aggiunta file)
git add "nome file"-> (aggiunta file)
git add . all-> (aggiunta di tutti i file)
git remote remove origin-> (rimuove link GITHUB)
git remote add origin "link GITHUB"-> (collegamento con GITHUB)       
git push -u origin master-> (invia il contenuto al GITHUB)
git remote -v-> (lista di server remoti)
git rm-> (rimuove i file dalla directory e nell'index)
git commit -m-> (messaggio che viene identificato)
git checkout -> (per tornare a uno stato precedente)
git checkout "nome branch"-> (per tornare in quella branch)
git branch-> (visualizza i branch presenti)
git branch "nome branch"-> (crea branch)
git branch -d "nome branch"-> (elimina il ramo)
git branch --merged-> (per vedere quali branch sono stati uniti)      
git remote -v -> (lista di server remoti)
git pull-> (scarica un file e lo mette in un altro)
git clone "link"-> (clona in una cartella)
git merge-> (unisce una o più cronologie di sviluppo)
git log-> (mostra i log di commit)
git log --oneline-> (mostra i log commit compattati)
git reset-> (per annullare una commit)
git reset --soft "codice del commit"-> (elimina quella commit)        
cat "nome file"-> (vedi cosa c'è scritto)
touch "nome file"-> (crea file)
git push origin "nome branch" ->aggiunge e salva su github